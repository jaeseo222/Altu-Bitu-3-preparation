#include <iostream>
#include <vector>
#include <algorithm>    // reverse()
using namespace std;
int main() {
    string a, b;    // ??©ö?? ?? ?? A, B?? ??????? ????
    cin>>a>>b;

    int i, j, p, q, num, carry=0;
    vector<int> ans;    // ???? ?? ?? ????
    
    i=a.length();  // ???…Z ????? A?? ?????
    j=b.length();  // ???…Z ????? B?? ?????

    while(i>0 && j>0) {
        p=a[--i]-'0'; q=b[--j]-'0';
        num=(p+q+carry)%10;
        carry=(p+q+carry)/10;   // ??????? ???? ?? ?????? ???? ?????
        ans.push_back(num);
    }
    while(i>0) {    // A?? ???? ???????? ???
        p=a[--i]-'0';
        num=(p+carry)%10;
        carry=(p+carry)/10;
        ans.push_back(num);
    }
    while(j>0) {    // B?? ???? ???????? ???
        q=b[--j]-'0';
        num=(q+carry)%10;
        carry=(q+carry)/10;
        ans.push_back(num);
    }
    if(carry) ans.push_back(carry); // ???? ???? 0?? ??? ???

    reverse(ans.begin(), ans.end()); // ??????????? ???? ??????????? vector ??????

    for(int i=0; i<ans.size(); i++) cout<<ans[i];
    return 0;
}